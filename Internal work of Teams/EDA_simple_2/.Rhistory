df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold"),
plot.margin = margin(t = 20, r = 80, b = 20, l = 80)
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
menus <- unique(sub_outcomes_by_menu$menu_group)
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
plot.title = element_text(
hjust = 0.5,
size = 16,
margin = margin(b = 10)
),
plot.margin = margin(t = 20, r = 80, b = 20, l = 80)  # <-- add more margin
)
+ ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
plot.title = element_text(
hjust = 0.5,
size = 16,
margin = margin(b = 10)
),
plot.margin = margin(t = 20, r = 80, b = 20, l = 80)  # <-- add more margin
)
+
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
plot.title = element_text(
hjust = 0.5,
size = 16,
margin = margin(b = 10)
),
plot.margin = margin(t = 20, r = 80, b = 20, l = 80)
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold"),
plot.margin = margin(r = 20, = 20)
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold"),
plot.margin = margin(r = 20, l = 20)
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
coord_polar(theta = "y", clip = "off") +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold")
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
coord_polar(theta = "y", clip = "off") +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold")
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
ggsave(
filename = paste0("graphics/menu_", menu_name, "_donuts.png"),
plot = p,       # <-- your combined 3-donut plot object
width = 12,     # adjust as needed
height = 4,
dpi = 300
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
coord_polar(theta = "y", clip = "off") +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold")
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
ggsave(
filename = paste0("graphics/menu_", menu_group, "_donuts.png"),
plot = p,       # <-- your combined 3-donut plot object
width = 12,     # adjust as needed
height = 4,
dpi = 300
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
coord_polar(theta = "y", clip = "off") +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold")
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
print(
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
ggsave(
filename = paste0("graphics/menu_", m, "_donuts.png"),
plot = p,       # <-- your combined 3-donut plot object
width = 12,     # adjust as needed
height = 4,
dpi = 300
)
}
for (m in menus) {
df_menu <- sub_outcomes_by_menu %>%
filter(menu_group == m)
# Function to build one donut per outcome
build_donut <- function(outcome_name, palette) {
df_out <- df_menu %>% filter(outcome == outcome_name)
ggplot(df_out, aes(x = 2, y = percent, fill = sub_type)) +
geom_col(width = 1, color = "white") +
scale_fill_manual(values = palette) +
coord_polar(theta = "y") +
xlim(0.5, 2.5) +
geom_text(
aes(
label = paste0(sub_type, " ", sprintf("%.1f%%", percent))
),
position = position_stack(vjust = 0.5),
size = 3.6,
fontface = "bold",
check_overlap = TRUE
) +
coord_polar(theta = "y", clip = "off") +
theme_void() +
theme(
legend.position = "none",
plot.title = element_text(hjust = 0.5, face = "bold")
) +
ggtitle(outcome_name)
}
# build the 3 donuts with distinct palettes
p_pos <- build_donut("Positive", pal_positive)
p_neu <- build_donut("Neutral",  pal_neutral)
p_neg <- build_donut("Negative", pal_negative)
# combine horizontally
row_plot <- p_pos + p_neu + p_neg + plot_layout(ncol = 3)
# print one row per menu
final_plot <- (
row_plot +
plot_annotation(
title = paste("Menu:", m),
theme = theme(
plot.title = element_text(size = 16, face = "bold", hjust = 0.5)
)
)
)
ggsave(
filename = paste0("graphics/menu_", m, "_donuts.png"),
plot = final_plot,       # <-- your combined 3-donut plot object
width = 12,     # adjust as needed
height = 4,
dpi = 300
)
}
outcomes_types |>
count(last_menu)
View(sub_outcomes_by_menu)
